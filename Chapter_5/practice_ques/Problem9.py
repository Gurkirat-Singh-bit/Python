# Problem 9) Can you change the values inside a list which is contained in set S?
# s = {8, 7, 12, "Harry", [1,2]}


# the thing is first of all we cannot have a list in the set and if,... only if python allow set can't be indexed
# so this is not possible to have a list in python and change it